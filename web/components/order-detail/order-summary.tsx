import type { RiskOrderDetail } from "@kuri/contracts";
import { cityLabel, dateTime, money, statusLabel } from "@/lib/formatters/operations-formatters";
import { RiskSummary } from "@/components/orders/risk-summary";
export function OrderSummary({ order }: { order: RiskOrderDetail }) { return <div className="grid gap-px border border-[var(--paper-200)] bg-[var(--paper-200)] sm:grid-cols-2 lg:grid-cols-4"><div className="bg-[var(--paper-50)] p-4"><p className="label">Estado actual</p><p className="mt-2 text-lg font-bold">{statusLabel(order.current_status)}</p></div><div className="bg-[var(--paper-50)] p-4"><p className="label">Ciudad / clima</p><p className="mt-2 text-sm">{cityLabel(order.city)} · {order.weather}</p></div><div className="bg-[var(--paper-50)] p-4"><p className="label">Hora prometida</p><p className="mt-2 text-sm tabular-nums">{dateTime(order.promised_at)}</p></div><div className="bg-[var(--paper-50)] p-4"><p className="label">Total / riesgo</p><p className="mt-2 text-sm">{money(order.total_amount_cents)}</p><div className="mt-2"><RiskSummary {...order.risk} /></div></div></div>; }
